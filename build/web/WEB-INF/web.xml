<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- Định nghĩa servlet cho ToyDetailServlet -->
    <servlet>
        <servlet-name>ToyDetailServlet</servlet-name>
        <servlet-class>controller.ToyDetailServlet</servlet-class>
    </servlet>

    <!-- Ánh xạ servlet đến URL /toydetailservlet -->
    <servlet-mapping>
        <servlet-name>ToyDetailServlet</servlet-name>
        <url-pattern>/toydetailservlet</url-pattern>
    </servlet-mapping>

    <!-- Định nghĩa filter cho Staff Authentication -->
    <filter>
        <filter-name>StaffAuthFilter</filter-name>
        <filter-class>filter.StaffAuthFilter</filter-class>
    </filter>

    <!-- Ánh xạ filter đến URL /staff/* -->
    <filter-mapping>
        <filter-name>StaffAuthFilter</filter-name>
        <url-pattern>/staff/*</url-pattern>
    </filter-mapping>

    <welcome-file-list>
        <welcome-file>home</welcome-file>
    </welcome-file-list>
</web-app>
